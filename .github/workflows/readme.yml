# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-03-24T12:23:30.524Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  push:
    branches:
      - main
    paths:
      - 'spec/sample.yml'

jobs:
  readme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v3

      - name: Validate open-api spec file
        uses: readmeio/rdme@v8
        with:
          rdme: openapi:validate spec/sample.yml
